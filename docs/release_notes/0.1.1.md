## fix
* 修复 GitHub Actions native 构建在 AOT 阶段缺少模型配置时失败的问题。
* AOT 处理期间若未加载到用户模型配置，会注入仅用于构建期的占位模型，避免构建机依赖真实模型端点、模型名或 API Key。
* AOT 处理期间跳过运行期初始化副作用，避免启动交互式 CLI、更新检查、Web/ACP 等运行时流程。

## verification
* 已验证 `./gradlew :soloncode-cli:processAot --stacktrace` 通过。

Full Changelog: https://github.com/Coooolfan/soloncode/compare/0.1.0...0.1.1
